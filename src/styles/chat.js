import { brightRed } from '../styles/colors';

export const badge = {
  backgroundColor: brightRed.hex,
  position: 'absolute',
  top: -10,
  left: -12,
  borderRadius: 100,
  paddingVertical: 3,
  paddingHorizontal: 8,
};

export const badgeText = {
  color: 'white',
};
